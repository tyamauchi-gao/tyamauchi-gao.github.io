# Github Pages テック記事要約ブログ
このリポジトリはtyamauchi-gaoのGithub Pages用のリポジトリです。
`tyamauchi-gao.github.io`

## こんなことがやりたい。
１、いくつかのリンクをClaude Codeに渡す。
２、Claude Codeがskillsを使って、その内容を要約して、マークダウン記事？もしくは適切な形式にする。
3、要約が終わったらgithub pagesの対象リポジトリにコミット、プッシュ。
4、github Actionsでデプロイし、要約記事が表示される。要約記事は例えば5つのリンクがあったら、5つのリンクの内容を今日の日付の記事の中に全部入れる感じ。


できるかな？
可能なら１では自分が渡したリストだけじゃなくて、zennとかqiitaとか海外のテックブロブとかのトピックをcliとかapiとか、MCP経由とかで取ってきて、それも追加で入れるとかできたらいいけど、それは難しければフェーズ２でもいいかな。

## github Pagesの作り方 by Gemini
正しいかわかりません。

GitHub Pagesですね。無料でWebサイトを公開できるので、エンジニアのポートフォリオやドキュメント公開には最高のツールです。

基本的には、GitHub上に**「リポジトリ」**を作り、そこにファイルをアップロードするだけでOKです。最もスタンダードな**「リポジトリを作成して公開する手順」**をまとめました。

---

### 1. リポジトリを作成する
1. GitHubにログインし、新しいリポジトリ（**New repository**）を作成します。
2. **Repository name** を決めます。
    * **ユーザーサイトにする場合：** `ユーザー名.github.io` という名前にします（これがそのままURLになります）。
    * **プロジェクトサイトにする場合：** 好きな名前（例：`my-project`）でOKです。
3. **Public**（公開）を選択して作成します。

### 2. ファイルをアップロードする
1. 作成したリポジトリに `index.html` という名前でメインのファイルを作成（またはアップロード）します。
    * ※GitHub Pagesは、この `index.html` をサイトのトップページとして認識します。

### 3. 公開設定を確認する
通常、`ユーザー名.github.io` という名前で作った場合は自動で公開されますが、念のため設定を確認しましょう。

1. リポジトリの **Settings** タブをクリック。
2. 左メニューの **Pages** を選択。
3. **Build and deployment** の項目で、Sourceが「Deploy from a branch」になっていることを確認。
4. Branchが `main`（または `master`）の `/ (root)` に設定されているか確認し、**Save** を押します。



---

### 公開URLのルール
公開されるURLは、リポジトリ名によって以下のようになります。

| 種類 | リポジトリ名 | 公開URL |
| :--- | :--- | :--- |
| **ユーザーサイト** | `username.github.io` | `https://username.github.io/` |
| **プロジェクトサイト** | `my-app` | `https://username.github.io/my-app/` |

---

### 💡 ヒント
* **反映までの時間:** 設定後、実際にサイトが見れるようになるまで数分かかることがあります。
* **独自ドメイン:** お金を出して買ったドメイン（.comなど）を割り当てることも可能です。

もし「HTMLファイルを直接いじるのは面倒」という場合は、**Jekyll（ジキル）** というテーマ機能を使って、デザインテンプレートから選んで作成することもできますよ。

まずは簡単な `index.html` を作ってアップロードしてみますか？それとも、ReactやVueなどのフレームワークで作ったものをデプロイしたい感じでしょうか？

